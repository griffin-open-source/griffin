/**
 * Runner configuration for target resolution.
 * Stores mappings from target keys to base URLs per organization and environment.
 */

/*
export interface RunnerConfig {
  id: string;
  organizationId: string;
  environment: string;
  targets: Record<string, string>;
  createdAt: Date;
  updatedAt: Date;
}
  */

/**
 * Input for creating a runner config
 */
//export interface CreateRunnerConfigInput {
//  organizationId: string;
//  environment: string;
//  targets: Record<string, string>;
//}
//
///**
// * Input for updating a runner config
// */
//export interface UpdateRunnerConfigInput {
//  targets?: Record<string, string>;
//}
//
///**
// * Query for finding runner configs
// */
//export interface RunnerConfigQuery {
//  organizationId?: string;
//  environment?: string;
//}
//
